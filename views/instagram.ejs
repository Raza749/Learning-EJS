<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Instagram</title>
    <style>
        img{
            height: 200px;
            width: 200px;
        }
    </style>
  </head>
  <body>
    <div>
      Instagram
      <h1>
        This page about @<%= data.name %>
      </h1>
      <button>
        Follow
      </button>
      <button>
        Message
      </button>
      <h4>
        Followers : <%= data.followers %> <br />
        Following : <%= data.following %>
      </h4>
      <hr />
      <div>
        <% for(let post of data.posts){ %>
        <img src="<%= post.image  %>" alt="some image">
        <span>
          Likes : <%= post.likes %>
        </span>
        <span>
          Comments : <%= post.comments %>
        </span>
        <% } %>
      </div>
    </div>
  </body>
</html>
